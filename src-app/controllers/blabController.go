package controllers

import (
	"net/http"
)

func showFeed(w http.ResponseWriter, r *http.Request) {
	// p, err := view.LoadPage(title)
	// if err != nil {
	// 	p = &Page{Title: title}
	// }
	// view.Render(w, "edit", p)
}
